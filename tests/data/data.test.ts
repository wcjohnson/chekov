import { describe, it } from "vitest";

describe("data layer", () => {
  it.todo("covers IndexedDB-backed query and mutation behavior");
  it.todo("verifies reminder/dependency/completion invariants");
});
